var user_experience_steps = function () {
    
    this.Then(/^I should be shown the tutorial$/, function(callback) {
      // express the regexp above with the code you wish you had
      callback.pending();
    });
    
};

module.exports = user_experience_steps;